# Body Shape Analyzer

This is a Next.js app for analyzing body shape from bust/chest, waist, and hip measurements.

## Development

Run the local dev server:

```bash
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000).
